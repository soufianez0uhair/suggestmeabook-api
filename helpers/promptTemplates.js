exports.booksSuggestionTemp = (input) => {
    return `give me 6 books names suggestions(only books title + by author ex. Les miserables by victor hugo) based on the following input and separate each book with a comma(don't use a numbered list and no period at the end). ${input}`
};